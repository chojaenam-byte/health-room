# CLAUDE.md — 보건실 대기 접수 시스템

## ⛔ 먼저 읽어라: 일하는 방식

공용 규칙은 기억 폴더의 `일하는_방식.md` 에 있다. 그 파일이 보이면 그것이 기준이다.
**원격(웹) 세션처럼 그 파일이 없는 환경을 위해 핵심만 여기 적는다.**

- 사용자에게는 **존댓말**로 답한다. 이 문서가 반말인 것은 나에게 주는 지시의 문체일 뿐이다.
  첫 답변부터 존댓말이다. 사용자가 반말로 물어도 마찬가지다.
- 문제를 발견하면 **고치고 → 테스트하고 → 두 줄로 보고한다**(무엇을 고쳤다 / 무엇으로 확인했다).
  허락을 기다리지 않는다. 먼저 확인받는 것은 **밖으로 나가는 일**(배포·공개·발송)뿐이다.
- **떠넘기지 마라.** ① 직접 해본다 → ② 막히면 무엇이 왜 막혔는지 근거를 댄다 → ③ 사람만 할 수 있는
  것(구글 계정 '허용' 클릭 등)만 부탁한다. 근거를 못 대면 아직 ①이 안 끝난 것이다.
  ⚠️ **원격 세션에서 푸시가 막히면 그건 그 컨테이너의 권한 문제다.** 사용자에게 앱 설치를 시키기 전에,
  집/학교 PC 세션에서 같은 일을 하면 그냥 된다는 것을 먼저 말해라(2026-09-20에 이걸 어겼다).
- **증거 없이 "됐다"고 쓰지 않는다.** 확인한 것과 못 한 것을 그대로 적는다.
- 사용자의 되물음("그거 왜 안 돼?")은 다시 설명하라는 뜻이 아니라 **다시 확인하라는 뜻**이다.

## 이 프로젝트

학교 보건실 대기 접수 시스템. **실제로 학생·교사가 쓰고 있다.**

- `main` = 운영본. `student.html`(키오스크), `teacher.html`(보건교사용).
- `v2` 브랜치의 `v2/` 폴더 = 개인정보 노출을 구조부터 고친 재작성본(`Code.gs`, `student.html`, `teacher.html`).
  2026-08-28 배포 완료, 살아 있다.
- 백엔드는 Google Apps Script. 프론트는 GitHub Pages.

## 손대기 전에

- **운영본(main)은 조심해서 다룬다.** 수업 중에 깨지면 보건교사가 접수를 못 받는다.
- 학생 개인정보(이름·알레르기 등)는 응답에 **넣지 않는다.** 가리는 게 아니라 아예 보내지 않는 쪽이다.
- 비밀번호·토큰은 이 저장소에 적지 않는다. Apps Script **스크립트 속성**에만 둔다.
- 코드 수정 후 배포: `cd v2 && clasp push --force && clasp deploy -i <배포ID>`.
  `push` 직후 바로 `deploy` 하면 반영이 안 될 때가 있으니 `clasp pull` 로 확인하고 배포한다.
  **사용자에게 편집기에 붙여넣으라고 시키지 마라.**

## ⛔ 아직 열려 있는 구멍 (2026-09-20 실측)

v1(`main`)의 옛 `/exec` 주소가 **아직 살아 있고, 로그인 없이 응답한다.**
익명 호출로 크기만 측정한 결과(내용은 열지 않았다):

- `?action=getStudents` → 200, 약 23KB
- `?action=getLogs` → 200, 약 287KB

즉 v2로 재작성한 의미가 지금은 없다. 공격자는 새 문이 아니라 열려 있는 옛 문으로 들어간다.
**이 배포를 내리는 것이 이 저장소의 1순위다.**

- v1 스크립트는 `jaenamking1@gmail.com` 드라이브에 **없다**(2026-09-20 확인). 다른 계정(학교 계정 등)
  소유라는 뜻이고, 그래서 이 PC의 clasp 자격으로는 내릴 수 없다.
- 내리는 법: 그 계정으로 Apps Script 편집기에서 해당 프로젝트를 열고 **배포 관리 → 보관(아카이브)**.
  내린 뒤 위 두 호출이 더 이상 200을 주지 않는지 확인한다.
